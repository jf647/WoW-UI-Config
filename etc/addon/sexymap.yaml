---
templates:
    global:
        sexymap:
            input: $TEMPLATEDIR/sexymap/SexyMap.lua.template
            output: $TEMPDIR/WTF/Account/$ACCOUNT/SavedVariables/SexyMap.lua
perchar_filter:
    exclude:
        - banker
        - mule
filter:
    include:
        - machine:name:kara
        - machine:name:heidi
